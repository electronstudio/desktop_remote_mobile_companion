# Built binaries at repo root only — anchored with / so they do not match
# the cmd/inara and cmd/inara_gui source directories.
/inara
/inara_gui
*.exe

# Local build outputs (make appimage, scripts/make-appimage.sh)
/build/
/dist/

# FFmpeg 8.1.2 built by the Makefile (source tree, objects, _install, tarball)
/ffmpeg-*/
/ffmpeg-*.tar.xz
server.crt
server.key
.idea
