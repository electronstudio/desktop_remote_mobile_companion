post_install() {
  cat <<'EOF'

  ==> desktop_remote_mobile_companion requires access to /dev/uinput for
      simulating input. Add your user to the uinput group:

          sudo usermod -aG uinput "$USER"

      Then reboot for the change to take effect.

EOF
}

post_upgrade() {
  post_install
}
