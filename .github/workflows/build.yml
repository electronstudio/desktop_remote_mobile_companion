name: Build

on:
  push:
  pull_request:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

env:
  FFMPEG_VERSION: "8.1.2"

jobs:
  get-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get-version.outputs.version }}
    steps:
      - uses: actions/checkout@v7
      - id: get-version
        run: |
          version=$(cat VERSION)
          echo "version=$version" >> "$GITHUB_OUTPUT"

  build-linux:
    needs: get-version
    runs-on: ubuntu-22.04

    env:
      FFMPEG_PREFIX: ${{ github.workspace }}/ffmpeg
      CGO_ENABLED: "1"

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - uses: actions/setup-go@v6
        with:
          go-version: "^1.26"

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            nasm \
            pkg-config \
            libdrm-dev \
            libva-dev

      - name: Cache FFmpeg build
        id: cache-ffmpeg
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/ffmpeg
          key: ffmpeg-${{ env.FFMPEG_VERSION }}-linux-ubuntu22.04-${{ hashFiles('.github/workflows/build.yml') }}

      - name: Build FFmpeg from source
        if: steps.cache-ffmpeg.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          curl -L -o /tmp/ffmpeg.tar.xz "https://ffmpeg.org/releases/ffmpeg-${{ env.FFMPEG_VERSION }}.tar.xz"
          tar -C /tmp -xJf /tmp/ffmpeg.tar.xz
          cd "/tmp/ffmpeg-${{ env.FFMPEG_VERSION }}"
          ./configure \
            --prefix="${{ env.FFMPEG_PREFIX }}" \
            --enable-static \
            --disable-shared \
            --enable-gpl \
            --enable-version3 \
            --enable-libdrm \
            --enable-vaapi \
            --disable-doc \
            --disable-programs \
            --disable-ffplay \
            --disable-ffprobe
          make -j"$(nproc)"
          make install

      - name: Build
        env:
          CGO_CFLAGS: "-I${{ env.FFMPEG_PREFIX }}/include"
          CGO_LDFLAGS: "-L${{ env.FFMPEG_PREFIX }}/lib"
          PKG_CONFIG_PATH: "${{ env.FFMPEG_PREFIX }}/lib/pkgconfig"
          PKG_CONFIG: "pkg-config --static"
        run: go build -o companion -ldflags "-linkmode external"

      - name: Stage artifact
        run: |
          name="desktop_remote_mobile_companion-linux-x86_64-${{ needs.get-version.outputs.version }}"
          mkdir -p "dist/$name"
          cp companion "dist/$name/"
          cp README.md LICENSE "dist/$name/"

      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          name: desktop_remote_mobile_companion-linux-x86_64-${{ needs.get-version.outputs.version }}
          path: dist/desktop_remote_mobile_companion-linux-x86_64-${{ needs.get-version.outputs.version }}


  build-windows:
    needs: get-version
    runs-on: windows-2022

    env:
      FFMPEG_PREFIX: /c/ffmpeg
      CGO_ENABLED: "1"

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          path-type: inherit
          update: true
          install: |
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-nasm
            mingw-w64-x86_64-pkg-config
            make
            diffutils
            patch

      - name: Cache FFmpeg build
        id: cache-ffmpeg
        uses: actions/cache@v4
        with:
          path: C:\ffmpeg
          key: ffmpeg-${{ env.FFMPEG_VERSION }}-windows-msys2-mingw64-${{ hashFiles('.github/workflows/build.yml') }}

      - name: Build FFmpeg from source
        if: steps.cache-ffmpeg.outputs.cache-hit != 'true'
        shell: msys2 {0}
        run: |
          set -euo pipefail
          mkdir -p /tmp
          curl -L -o /tmp/ffmpeg.tar.xz "https://ffmpeg.org/releases/ffmpeg-${FFMPEG_VERSION}.tar.xz"
          tar -C /tmp -xJf /tmp/ffmpeg.tar.xz
          cd "/tmp/ffmpeg-${FFMPEG_VERSION}"

          # Work around Windows command-line length limits when archiving
          # many object files into the static libraries.
          patch -p1 <<'PATCH' || true
          diff --git a/ffbuild/library.mak b/ffbuild/library.mak
          index 793e9d41fa..e3c15e1d67 100644
          --- a/ffbuild/library.mak
          +++ b/ffbuild/library.mak
          @@ -35,7 +35,8 @@ OBJS += $(SHLIBOBJS)
           endif
           $(SUBDIR)$(LIBNAME): $(OBJS) $(STLIBOBJS)
          	$(RM) $@
          -	$(AR) $(ARFLAGS) $(AR_O) $^
          +	$(file > objs.txt, $^)
          +	$(AR) $(ARFLAGS) $(AR_O) @objs.txt
          	$(RANLIB) $@

           install-headers: install-lib$(NAME)-headers install-lib$(NAME)-pkgconfig
          PATCH

          ./configure \
            --prefix="${FFMPEG_PREFIX}" \
            --enable-static \
            --disable-shared \
            --enable-gpl \
            --enable-version3 \
            --disable-doc \
            --disable-programs \
            --disable-ffplay \
            --disable-ffprobe
          make -j"$(nproc)"
          make install

      - uses: actions/setup-go@v6
        with:
          go-version: "^1.26"

      - name: Set Windows build environment
        shell: msys2 {0}
        run: |
          echo "CGO_CFLAGS=-I/c/ffmpeg/include" >> $GITHUB_ENV
          echo "CGO_LDFLAGS=-L/c/ffmpeg/lib -lntdll -lsetupapi" >> $GITHUB_ENV
          echo "PKG_CONFIG=pkg-config --static" >> $GITHUB_ENV

      - name: Verify FFmpeg pkg-config files
        shell: msys2 {0}
        run: |
          export PKG_CONFIG_PATH="/c/ffmpeg/lib/pkgconfig"
          echo "PKG_CONFIG_PATH=$PKG_CONFIG_PATH"
          which pkg-config
          pkg-config --version
          ls -la /c/ffmpeg/lib/pkgconfig/ || true
          pkg-config --list-all | grep -i '^libav' || true
          pkg-config --cflags libavcodec || true

      - name: Build
        shell: msys2 {0}
        run: |
          export PKG_CONFIG_PATH="/c/ffmpeg/lib/pkgconfig"
          go build -ldflags "-linkmode external" -o companion.exe

      - name: Stage artifact
        shell: msys2 {0}
        run: |
          name="desktop_remote_mobile_companion-windows-x86_64-${{ needs.get-version.outputs.version }}"
          mkdir -p "dist/$name"
          cp companion.exe "dist/$name/"/
          cp README.md LICENSE "dist/$name/"

      - name: Upload binary
        uses: actions/upload-artifact@v7
        with:
          name: desktop_remote_mobile_companion-windows-x86_64-${{ needs.get-version.outputs.version }}
          path: dist/desktop_remote_mobile_companion-windows-x86_64-${{ needs.get-version.outputs.version }}

#  build-flatpak:
#    needs: get-version
#    runs-on: ubuntu-24.04
#
#    steps:
#      - uses: actions/checkout@v7
#
#      - name: Install flatpak
#        run: |
#          sudo apt-get update -qq
#          sudo apt-get install -y flatpak flatpak-builder appstream
#          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
#          flatpak install --user -y flathub \
#            org.freedesktop.Platform//25.08 \
#            org.freedesktop.Sdk//25.08 \
#            org.freedesktop.Sdk.Extension.golang//25.08
#
#      - name: Build flatpak
#        run: |
#          flatpak-builder --user --repo repo --force-clean build-dir flatpak/uk.co.electronstudio.desktop_remote_mobile_companion.yaml
#          flatpak build-bundle repo low_latency_dictation.flatpak uk.co.electronstudio.desktop_remote_mobile_companion
#
#      - name: Stage artifact
#        run: |
#          name="desktop_remote_mobile_companion-flatpak-x86_64-${{ needs.get-version.outputs.version }}"
#          mkdir -p "dist/$name"
#          cp low_latency_dictation.flatpak "dist/$name/"
#          cp README.md LICENSE "dist/$name/"
#
#      - name: Upload bundle
#        uses: actions/upload-artifact@v7
#        with:
#          name: desktop_remote_mobile_companion-flatpak-x86_64-${{ needs.get-version.outputs.version }}
#          path: dist/desktop_remote_mobile_companion-flatpak-x86_64-${{ needs.get-version.outputs.version }}

  combine:
    needs: [get-version, build-linux, build-windows]
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: desktop_remote_mobile_companion-*-${{ needs.get-version.outputs.version }}

      - name: Build combined archives
        run: |
          version="${{ needs.get-version.outputs.version }}"
          outdir="dist/desktop_remote_mobile_companion-all-$version"
          mkdir -p "$outdir"
          for dir in artifacts/desktop_remote_mobile_companion-*; do
            name=$(basename "$dir")
            if [[ "$name" == desktop_remote_mobile_companion-linux-* ]]; then
              chmod +x "artifacts/$name/companion"
              (cd artifacts && tar -cJf "../$outdir/$name.tar.xz" "$name")
            else
              (cd artifacts && zip -r "../$outdir/$name.zip" "$name")
            fi
          done

      - name: Upload combined artifact
        uses: actions/upload-artifact@v7
        with:
          name: desktop_remote_mobile_companion-all-${{ needs.get-version.outputs.version }}
          path: dist/desktop_remote_mobile_companion-all-${{ needs.get-version.outputs.version }}
